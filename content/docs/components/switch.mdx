---
title: Switch
description: A draggable toggle with spring-based thumb animation.
---

<ComponentPreview name="switch/default" />

## Installation

<CodeTabs>
  <TabsList>
    <TabsTrigger value="cli">Command</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.iandefined.com/r/switch.json
```

  </TabsContent>
  <TabsContent value="manual">

<Steps>
  <Step>Install the following dependencies:</Step>

```bash
npm install @base-ui/react framer-motion clsx tailwind-merge
```

<Step>Add the `cn` utility to `lib/utils.ts`:</Step>

```ts filename="lib/utils.ts"
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="src/registry/base/switch.tsx"
  title="components/ui/switch.tsx"
/>
</Steps>

  </TabsContent>
</CodeTabs>

## Usage

```tsx
import { Switch } from "@/components/ui/switch";
```

```tsx
const [checked, setChecked] = useState(false);

<Switch
  aria-label="Enable notifications"
  checked={checked}
  onCheckedChange={setChecked}
/>;
```

## Examples

### With Label

<ComponentPreview name="switch/with-label" />

### Disabled

<ComponentPreview name="switch/disabled" />

### Sizes

<ComponentPreview name="switch/sizes" />

### Controlled

<ComponentPreview name="switch/controlled" />

### Custom Card Style

<ComponentPreview name="switch/custom-card-style" />
