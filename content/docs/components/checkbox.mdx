---
title: Checkbox
description: A control that allows the user to toggle between checked and unchecked.
---

## Preview

<ComponentPreview name="checkbox/default" />

## Installation

<CodeTabs>
  <TabsList>
    <TabsTrigger value="cli">Command</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">

```bash
pnpm dlx shadcn@latest add https://ui.iandefined.com/r/checkbox.json
```

  </TabsContent>
  <TabsContent value="manual">

<Steps>
  <Step>Install the following dependencies:</Step>

```bash
pnpm add class-variance-authority clsx tailwind-merge @base-ui/react
```

<Step>Add the `cn` utility to `lib/utils.ts`:</Step>

```ts filename="lib/utils.ts"
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  src="src/registry/base/checkbox.tsx"
  title="components/ui/checkbox.tsx"
/>
</Steps>

  </TabsContent>
</CodeTabs>

## Anatomy

```tsx
import {
  Checkbox,
  CheckboxIndicator,
  CheckboxRoot,
} from "@/components/ui/checkbox";

// Option #1. Basic usage:
<Checkbox />;

// Option #2. Custom usage:
<CheckboxRoot>
  <CheckboxIndicator />
</CheckboxRoot>;
```

## Usage

### Disabled

<ComponentPreview name="checkbox/disabled" />

### Sizes

<ComponentPreview name="checkbox/sizes" />

### Radius

<ComponentPreview name="checkbox/radius" />

### Custom Icons

<ComponentPreview name="checkbox/icons" />
